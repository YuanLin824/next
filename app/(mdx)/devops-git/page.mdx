# Git

```shell
# 将分支提交汇总为一次提交合并到 master
git checkout master
git merge --squash 分支名称
git commit -m '汇总后的一次commit内容'
```

## git config

```shell
# 查看 git 全局配置
git config --global --list
# 设置全局用户名称
git config --global user.name Yuan
# 设置全局 lf
git config --global core.autocrlf input
# 设置全局用户邮箱
git config --global user.email 2752137708@qq.com
# git status 等命令自动着色
git config --global color.ui true
git config --global color.status auto
git config --global color.diff auto
git config --global color.branch auto
git config --global color.interactive auto
# remove  proxy configuration on git
git config --global --unset http.proxy
```

## git fetch & git pull & git push

```shell

```

## git stash & git add & git rm & git mv & git status

```shell

```

## git merge & git rebase

```shell
# 合并远程master分支至当前分支
git merge origin/master

# 衍合指定分支到当前分支
git rebase <branch>
```

## git reset & git revert & git commit

```shell

```

## git remote

```shell
# 查看远程版本库信息
git remote -v
# 查看指定远程版本库信息
git remote show <remote>
# 添加远程版本库
git remote add <remote> <url>
```

## git checkout

```shell
# 切换到指定分支或标签
git checkout <branch/tag>
# 从当前分支创建新分支 (master_copy) 并检出
git checkout -b master_copy
# 从指定分支创建新分支 (master_copy) 并检出
git checkout -b master master_copy
# 检出已存在的features/performance分支
git checkout features/performance
# 检出远程分支hotfixes/BJVEP933并创建本地跟踪分支
git checkout --track hotfixes/BJVEP933
# 检出版本v2.0
git checkout v2.0
# 从远程分支develop创建新本地分支devel并检出
git checkout -b devel origin/develop
# 检出head版本的README文件（可用于修改错误回退）
git checkout -- README
```

## git branch

```shell
# 显示所有本地分支
git branch
# 创建新分支
git branch <new-branch>
# 显示包含提交50089的分支
git branch --contains 50089
# 显示所有分支
git branch -a
# 显示所有原创分支
git branch -r
# 显示所有已合并到当前分支的分支
git branch --merged
# 显示所有未合并到当前分支的分支
git branch --no-merged
# 本地分支改名
git branch -m master master_copy
# 删除本地分支
git branch -d <branch>
```

## git tag

```shell
# 列出所有本地标签
git tag
# 基于最新提交创建标签
git tag <tagname>
# 删除标签
git tag -d <tagname>
```

## git log & git show & git blame & git diff

```shell
# 显示提交日志
git log
# 显示 n(n 为数字) 行日志
git log -n
# 显示提交日志及相关变动文件
git log --stat
git log -p -m
# 以列表方式查看指定文件的提交历史
git blame <file>
```
